let y:unknown="hello";
console.log((y as string).length);//as i a casting where that specify the varible  name<> also acts as castng
//as we can use it laike this but we cant change th evalue which is nuber into string